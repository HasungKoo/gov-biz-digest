# gov-biz-digest — 프로젝트 규칙

> 중소기업 대상 IT·창업·AI 분야 정부 지원사업 공고를 매일 수집해
> Telegram으로 보고하고 웹페이지로 공개한다.
>
> 이 문서는 에이전트와 사람이 함께 지키는 규칙이다.
> API 키·토큰·서비스키는 이 문서에 포함하지 않는다.

---

## 0. 이 프로젝트의 성격

유튜브 다이제스트가 틀리면 영상 하나를 놓친다.
**공고 다이제스트가 틀리면 마감을 놓친다.**

실제 손해가 발생하는 쪽이므로, 편의보다 정확성을 우선한다.
"그럴듯하게 틀린 정보"가 이 프로젝트에서 가장 위험하다.

---

## 1. 데이터 무결성

### 1-1. 명세가 아니라 실제 응답을 기준으로 한다

기업마당 API 문서와 실제 응답은 다르다. 확인된 차이:

- 문서의 필수(Y) 필드 `title` `link` `seq` `author` 는 실제 응답에 **없다.**
  실제로는 `pblancNm` `pblancUrl` `pblancId` `jrsdInsttNm` 이 온다.
- 신청기간 샘플은 `20220727 ~ 20220930` 이지만
  실제는 `2026-09-03 ~ 2026-09-28` (하이픈 포함)이다.

**필드를 추가하거나 파싱 규칙을 바꿀 때는 반드시 실제 응답을 다시 받아 확인한다.**
문서만 보고 코드를 고치지 않는다.

### 1-2. 파싱 실패값은 추측하지 않고 원문을 그대로 표시한다

신청기간은 자유 입력이다. 2026-09-07 기준 전체 1,550건 중 **912건(59%)** 이
날짜 형식이 아니며, 표기는 **33종**이다.

```
예산 소진시까지 / 예산 소진시 까지 / 예산 소진 시까지   ← 띄어쓰기만 다름
상시 접수 / 수시 접수 / 수시 모집 / 연중 상시 모집
선착순 접수 / 선착순 마감 / 선순 마감
세부사업별 상이 / 차수별 상이 / 회차별 상이 / 분기별 상이 ...
```

자유 입력이므로 **다음 달에 34번째 표기가 생긴다.**
표기를 하나씩 매핑하지 않는다. 세 갈래로만 처리한다.

| 판정 | 처리 |
| --- | --- |
| 날짜 두 개 파싱 성공 | D-day 계산, 마감 임박순 정렬 |
| 파싱 실패 | `상시·미정`으로 묶고 **원문 문자열을 그대로 노출** |
| 형식 변형 (`2020.01.01 ~ ...`) | 구분자를 넓게 받아 날짜로 편입 |

**"예산 소진시까지"를 화면에 그대로 쓰는 것이 억지 분류보다 정확하다.**
사용자가 그 표현을 보고 스스로 판단할 수 있어야 한다.

### 1-3. 원본을 보존한다

- 수집한 JSON 원본을 타임스탬프 폴더에 그대로 저장하고 SHA-256을 기록한다.
- manifest에 요청 조건·건수·해시·수집 시각을 남긴다.
- **manifest에 인증키 문자열을 기록하지 않는다.** (`api_key_recorded: false`)
- 원본·정규화 결과·보고 산출물을 구분해 보존한다.

### 1-4. 링크 없는 항목은 보고하지 않는다

`pblancUrl` 이 없는 공고는 버린다.
확인할 수 없는 정보를 전달하는 것이 누락보다 나쁘다.

---

## 2. 보고 무결성

### 2-1. 사실은 API 필드를 그대로 옮긴다

마감일, 지원대상, 소관기관, 신청방법은 **응답 값을 그대로 쓴다.**
요약하거나 다듬지 않는다. LLM이 만들어내지 않는다.

유튜브 프로젝트에서 LLM을 파이프라인에서 뺀 이유는 장애 회피였다.
이 프로젝트에서는 **정확성**이 이유다. 결론은 같다 —
**cron 실행 경로에 LLM을 두지 않는다.**

### 2-2. 적합 판정을 하지 않는다

"이 공고는 귀사에 적합합니다" 같은 판단을 출력하지 않는다.

자격요건은 공고 원문 PDF를 봐야 확인할 수 있고, 틀리면 대가가 크다.
**키워드로 걸러주되, 적합 여부 판단은 사람이 한다.**

### 2-3. 기준시각을 함께 기록한다

모든 보고에 다음을 남긴다.

- `as_of_time` — D-day 계산 기준시각 (KST)
- `snapshot_id` — 어느 수집분을 근거로 했는지
- `compare_with` — 신규 판정에 쓴 직전 수집분

`D-3` 만 있으면 오늘 것인지 어제 것인지 알 수 없다.

### 2-4. 신규 판정 근거를 남긴다

`pblancId` 를 기준으로 직전 수집분과 비교해 신규를 판정한다.
다른 소스(K-Startup 등)를 추가해 ID 체계가 섞이면,
**어떤 규칙으로 두 공고를 같다고 묶었는지 manifest에 기록한다.**
나중에 "왜 이 공고가 안 보이지"를 추적할 유일한 실마리다.

---

## 3. 출처와 저작권

- 데이터 출처는 기업마당이다. 응답의 `copyright` 필드에 `bizinfo` 로 온다.
- **웹페이지에 출처를 표기하지 않고 공고 내용을 게시하지 않는다.**
- 공고 본문(`bsnsSumryCn`)에는 HTML 태그와 `&nbsp;` `\r\n` 이 섞여 있다.
  태그를 제거해 표시하되, **내용을 임의로 줄이거나 바꾸지 않는다.**

---

## 4. 보안

- `.gitignore` 가 `.env`, `.venv/`, `data/raw/`, `docs/*.pdf` 를 제외한다.
- push 전에 확인한다.

```bash
git ls-files | grep -i env    # 아무것도 안 나와야 정상
```

- 인증키·봇 토큰을 출력하거나 로그에 남기지 않는다.
- 저장소는 Public이다. 키가 올라가면 즉시 재발급한다.

---

## 5. 검증

- 코드 변경 후 `pytest -q` 를 돌린다.
- **통과만으로는 부족하다.** 일부러 깨뜨려 실패가 나오는지 확인한다.
- 실험 전에 커밋해 안전지대를 만든다.
  커밋 안 한 파일은 `git checkout` 으로 되돌릴 수 없다.
- 순수 함수(날짜 파싱, 키워드 필터, 신규 판정)를 우선 검증한다.
  API를 호출하는 테스트는 만들지 않는다.

### 서브에이전트 검증

지적을 검증할 때 **인용한 줄 번호와 코드 원문을 그대로 보고에 넣는다.**
근거 원문 없이 "모든 지적이 사실"이라고 쓰지 않는다.
중단 후 재시도할 때는 이전 결과를 재사용하지 않고 대상 파일을 처음부터 다시 읽는다.

---

## 6. 실패 격리

단계별로 실패를 가둔다. 앞 단계가 죽어도 뒤가 살아야 한다.

| 단계 | 실패 시 |
| --- | --- |
| 수집 | 마지막 저장분으로 보고 시도 |
| Telegram 보고 | 재시도 후 중단 (가장 중요한 산출물) |
| HTML 생성 | 보고는 이미 나갔으므로 경고만 |
| git push | 인증 문제 안내 |

스크립트는 실패해도 `exit 0` 으로 끝내고 **오류를 표준출력에 남긴다.**
출력이 비면 "실패한 건지 잊은 건지" 알 수 없게 된다.

### Telegram은 직접 호출한다

Hermes gateway의 `--deliver` 를 쓰지 않고 스크립트가 Bot API를 직접 호출한다.
재시도 횟수와 네트워크 대기를 직접 정하기 위해서다.
노트북이 절전에서 깬 직후라는 조건을 아는 것은 스크립트 쪽이다.

---

## 7. 하드코딩 금지

- 검색 키워드, 기간, 보고 개수, 분야를 코드에 박지 않는다.
- 모두 명령행 인자 또는 설정 파일로 뺀다.
- 키워드 목록을 바꾸면 **그 이유와 실제 건수 변화를 문서에 남긴다.**

---

## 8. 작업 환경

- 프로젝트를 옮길 때 `deactivate` 후 그 프로젝트의 `.venv` 를 활성화한다.
  안 그러면 엉뚱한 환경에서 테스트가 깨진다. 코드 문제처럼 보이는 환경 문제다.
- 100줄 넘는 파일은 터미널 붙여넣기로 만들지 않는다. 파일로 받아 `cp` 한다.
- `.env` 를 고친 뒤에는 `source .env` 를 다시 한다.
- 붙여넣은 뒤 확인한다: `wc -l`, `tail -3`, `python -m py_compile`

---

## 9. 문서 갱신

- 중요 단계가 완료될 때마다 요약본의 "현재 위치"와 "다음 작업"을 갱신한다.
- Git 커밋 해시, 브랜치, pytest 결과, cron 작업 ID, 공개 주소를 기록한다.
- 프로필·cron·gateway·Vercel 설정은 Git 밖에 있으므로 요약본이 유일한 재현 근거다.
- API 키·토큰·개인식별정보는 복사하지 않는다.

---

*확인 기준일 2026-09-07 · 기업마당 지원사업정보 API 전체 1,550건 기준*
